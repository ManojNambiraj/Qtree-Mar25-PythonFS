// Operators:

    // 1. Arithmetics OP

        // (+, -, *, /, %, ++, --, **)

            // let a = "hello"
            // let b = "John"

            // const result = a + " " + b

            // console.log(result);

            // let a = 10;
            // let b = 5;

            // const result = a + b;
            // const result = a - b;
            // const result = a * b;
            // const result = a / b;
            // const result = a % b;
            // const result = ++a
            // const result = --a
            // const result = b ** 3

            // console.log(result);

    // 2. Assignment OP

        // (=, +=, -=, *=, /=, %=, **=)

        // let a = 5

        // a += 2    // a = a + 2
        // a -= 2    // a = a - 2
        // a *= 2    // a = a * 2
        // a /= 2    // a = a / 2
        // a %= 2    // a = a % 2
        // a **= 2    // a = a ** 2

        // console.log(a);

    // 3. Comparison OP ---> (Boolean values -> True or false)

        // (==, ===, !=, !==, <, <=, >, >=)

        // let age = 18

        // const result = (age == 18)
        // const result = (age === 18)
        // const result = (age != 18)
        // const result = (age !== 18)
        // const result = (age < 18)
        // const result = (age <= 18)

        // console.log(result);

    // 4. Logical OP

        // (&&, ||, !)

        // &&

            // (True)  && (True)   --> True
            // (True)  && (False)  --> False
            // (False) && (True)   --> False
            // (False) && (False)  --> False

        // ||

            // (True)  || (True)   --> True
            // (True)  || (False)  --> True
            // (False) || (True)   --> True
            // (False) || (False)  --> False

        // !

            // (True)  --> False
            // (False) --> True

        // let age = 18

        // const result = (age == 18) && (age <= 18)
        // const result = (age == 18) || (age < 18)

        // console.log(!result);
        
    // 5. Bitwise OP
    // 6. Ternary OP

        // let age = 120

        // const result = (age == 18) ? "Eligible" : "Not Eligible"

        // console.log(result);
        
    // 7. Type OP

        let a = 85

        console.log(a, typeof(a));
        